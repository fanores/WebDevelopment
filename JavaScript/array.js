/* Working with arrays. */
var nameList = ["Stevo", "Simon"];
var userName = prompt("Enter your name:");


if (nameList.includes(userName)) {
    console.log("Welcome.");
} else {
    console.log("Not Welcome.");
}
