/* Determine user's string length input. */
var maxCharacters = 100;
var mySentence = prompt("Type your sentence:");
alert("You have written: " + mySentence.length + " characters out of: " + (maxCharacters - mySentence.length));
